# iFood consumer probe — 2026-09-01

Unauthenticated Node fetch, browser Origin. KEEP = JSON HTTP 200/400/401.

| Path | Result | Ship |
| --- | --- | --- |
| GET `/v1/customers/me/external-identities` | 401 JSON `no jwt token` | `ifood_identities` |
| GET `/v4/customers/me/orders?status=ONGOING` | 401 JSON | `ifood_list_active_orders` |
| GET `/v3/customers/me/orders/:id` | already KEEP (existing `ifood_get_order`) | track/eta/receipt/invoice compose this |
| POST `/v1/customers/me/addresses` | 401 JSON | `ifood_create_address` |
| POST `cw-marketplace /v1/carts` | 401 JSON | `ifood_add_to_cart` |
| PUT/DELETE `/v1/customers/me/addresses/:id` | 404 JSON | honest gap |
| POST/PUT order `:id/reorder\|cancel` | 404 JSON | honest gap |
| PUT/PATCH/DELETE `/v1/carts/:id` and `/coupon` `/tip` | 404 JSON | honest gap |
| geocode / favorites / catalog items | 404 JSON | honest gap |
| merchant GraphQL catalog extras / menu REST | Akamai HTML 403 or 404 JSON | honest gap — not shipped as `ifood_merchant_catalog` |

Evidence: `{SCRATCH}/probes/all.json` + `followup.json`.
